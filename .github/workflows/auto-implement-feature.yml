name: Auto-Implement Feature

on:
  workflow_dispatch:
    inputs:
      feature_id:
        description: 'Feature ID from feature_matrix.yaml (e.g., ui.app_shell)'
        required: true
        type: string
      use_copilot_agent:
        description: 'Use GitHub Copilot coding agent for implementation'
        required: false
        type: boolean
        default: true

jobs:
  auto-implement:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
      issues: write
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.REPO_TOKEN }}

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pyyaml requests

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          flutter-version: '3.29.2'

      - name: Flutter pub get
        run: flutter pub get

      - name: Validate feature exists
        run: |
          python -c "
          import yaml
          with open('feature_matrix.yaml') as f:
              data = yaml.safe_load(f)
          features = [f['id'] for f in data.get('features', [])]
          if '${{ github.event.inputs.feature_id }}' not in features:
              print('❌ Feature not found in feature_matrix.yaml')
              exit(1)
          print('✅ Feature found')
          "

      - name: Configure Git
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'

      - name: Pre-execution health check
        run: |
          python scripts/automation_monitor.py check --json > health_check.json
          CRITICAL_COUNT=$(python -c "
          import json
          with open('health_check.json') as f:
              data = json.load(f)
          critical = len([i for i in data['issues'] if i.startswith('❌')])
          print(critical)
          ")
          
          if [ "$CRITICAL_COUNT" -gt "0" ]; then
            echo "❌ Critical issues detected, aborting automation"
            python scripts/automation_monitor.py check
            exit 1
          fi
          
          echo "✅ Health check passed"

      - name: Auto-implement feature
        id: implement
        run: |
          # Monitor execution
          python scripts/automation_monitor.py monitor --feature-id ${{ github.event.inputs.feature_id }} --json > monitor_result.json &
          MONITOR_PID=$!
          
          # Run implementation
          python scripts/auto_implement_feature.py ${{ github.event.inputs.feature_id }} --push
          
          # Stop monitoring
          kill $MONITOR_PID 2>/dev/null || true
          
          echo "branch_name=feature/${{ github.event.inputs.feature_id }}" | tr '.' '-' >> $GITHUB_OUTPUT

      - name: Post-execution validation
        if: always()
        run: |
          # Check if files were created
          if [ ! -d "lib/features/${{ github.event.inputs.feature_id }}" ]; then
            echo "❌ Feature directory not created"
            exit 1
          fi
          
          # Validate generated code
          flutter analyze || echo "⚠️ Analysis warnings found"
          
          # Run tests with coverage
          flutter test --coverage || echo "⚠️ Test issues found"
          
          echo "✅ Validation completed"

      - name: Check coverage quality
        if: always()
        run: |
          if [ -f "coverage/lcov.info" ]; then
            # Use our monitoring script to check coverage
            python scripts/automation_monitor.py check --json > automation_check.json
            
            # Extract coverage issues
            COVERAGE_ISSUES=$(python -c "
            import json
            with open('automation_check.json') as f:
                data = json.load(f)
            issues = [i for i in data.get('issues', []) if 'Coverage' in i]
            if issues:
                print('Coverage issues detected:')
                for issue in issues:
                    print(f'  {issue}')
            else:
                print('Coverage validation passed')
            ")
            echo "$COVERAGE_ISSUES"
          else
            echo "⚠️ No coverage report generated"
          fi

      - name: Upload coverage reports
        if: always()
        uses: codecov/codecov-action@v5
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: coverage/lcov.info
          flags: automated-feature
          name: auto-implementation-${{ github.event.inputs.feature_id }}
          fail_ci_if_error: false

      - name: Upload monitoring data
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: automation-monitoring-${{ github.run_id }}
          path: |
            health_check.json
            monitor_result.json
            automation_check.json
            automation_monitor.log
            coverage/lcov.info

      - name: Create Pull Request
        id: create_pr
        env:
          GH_TOKEN: ${{ secrets.REPO_TOKEN }}
        run: |
          FEATURE_ID="${{ github.event.inputs.feature_id }}"
          BRANCH_NAME=$(echo "feature/${FEATURE_ID}" | tr '.' '-')
          
          FEATURE_TITLE=$(python -c "
          import yaml
          with open('feature_matrix.yaml') as f:
              data = yaml.safe_load(f)
          for f in data.get('features', []):
              if f['id'] == '${{ github.event.inputs.feature_id }}':
                  print(f['title'])
                  break
          ")
          
          cat > pr_body.md << 'EOF'
          ## Auto-Generated Feature Implementation
          
          This PR contains the automatically generated scaffold for the feature.
          
          ### What's Included
          - ✅ Feature scaffold with Clean Architecture structure  
          - ✅ Generated models and providers
          - ✅ Basic repository interfaces
          - ✅ Acceptance tests placeholder
          - ✅ Implementation plan
          
          ### Ready for GitHub Copilot Implementation
          EOF
          
          gh pr create \
            --title "feat: implement ${FEATURE_TITLE}" \
            --body-file pr_body.md \
            --label "feature" \
            --head "${BRANCH_NAME}" \
            --base main

  notify:
    needs: auto-implement
    runs-on: ubuntu-latest
    steps:
      - name: Notify completion
        run: |
          echo "🎉 Feature implementation started!"
          echo "📋 Check the pull request for progress"
          echo "🤖 GitHub Copilot coding agent will complete the implementation"
